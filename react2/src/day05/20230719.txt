
2023-07-19(수)

리스트 : 목록을 뜻하는 리스트, 자바스크립트에서는 리스트가 없기 때문에 배열을 뜻함
    -->> 리스트에 컴포넌트를 저장하고 반복문으로 리스트 출력

    map 사용법

    ::: 배열.map((변수명(필수), index, 현재사용하고 있는 배열)) => {

            실행문

        return 출력;
        }

키 : 각 객체나 아이템이나 구분할 수 있는 유일한 값

제어컴포넌트 : 사용자가 입력한 값에 접근하고 제어할 수있도록 해주는 컴포넌트
    - 제어 컴포넌트는 그 값이 리액트의 통제를 받는 입력 폼 엘리먼트

props : 부모에서 자식으로 일반통행이다. 전달하기 위해서는 매개변수를 통해서 전달해야 한다.

shared state : 공유된 state, 자식 컴포넌트가 공통된 부모 컴포넌트의 state를 공유하여 사용하는 것
            - 어떤 컴포넌트의 state에 있는 데이터를 여러개의 자식 컴포넌트에서 공통적으로 사용하는 것
            - 부모 컴포넌트 의 값을 자식 컴포넌트 에게 값을 전달(공유) 해주는 state 을 말한다.
            - 자식 컴포넌트의 state 가져오기 :  <자식컴포넌트 데이터 = xxx, 속성=함수() >